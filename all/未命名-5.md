```mermaid
erDiagram
    AI_CHAT_BOT {
        bigint id PK "主键ID"
        varchar(256) bot_id UK "机器人标识"
        varchar(128) name "机器人名称"
        varchar(32) bot_type "类型: Master/Slave"
        longtext prompt "提示词信息"
        varchar(64) prompt_variables "提示词变量"
        longtext prefix_prompt "前缀提示词"
        longtext suffix_prompt "后缀提示词"
        int memory_limit_size "长程记忆限制"
        varchar(256) welcome_tip "欢迎语"
        int vector_top_k "向量Top K值"
        varchar(256) namespace_id "知识库空间ID"
        ---
        tinyint deleted "逻辑删除标志"
        varchar(32) creator "创建人"
        datetime create_time "创建时间"
        varchar(32) updator "更新人"
        datetime update_time "更新时间"
        int version "版本号"
        ---
        varchar(256) origin_domain "来源域名"
        varchar(128) slave_bot_mark "从节点标记"
        varchar(256) fixed_ques "固定问题"
        int use_type "归属类型:0私有/1公有"
        int traceability "溯源开关"
        int fragmented_relationship "分片血缘树"
        varchar(255) slave_carrying_question "携带主节点问题"
        varchar(255) llms "关联大模型"
        varchar(255) llms_base "子知识库"
        int prompt_ability "召回分片开关"
    }

```